const APP_NAME = 'آوازاد'

const MIN_EXPRESSIONS_NO = 1000
// This is not our preference. But rather MCV requires
// at least 1000 sentences/expressions in a TSV.

export {
  APP_NAME,
  MIN_EXPRESSIONS_NO,
}
